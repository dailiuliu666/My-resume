# [姓名]

<table>
<tr><td>求职意向</td><td>[Go 后端 / 全栈开发工程师]</td></tr>
<tr><td>电话</td><td>[手机号]</td></tr>
<tr><td>邮箱</td><td>[邮箱]</td></tr>
<tr><td>所在地</td><td>[城市]</td></tr>
<tr><td>GitHub</td><td>https://github.com/earthcake2233/cakecake</td></tr>
</table>

## 个人简介

具备 Go + Vue 3 全栈开发能力，能够独立完成从需求建模、核心链路设计到部署运维的闭环。重点实践实时通信、异步任务、搜索、AI Agent 及高可用工程化，关注一致性、幂等、可观测性与性能验证。

## 技术能力

- **后端**：Go、Gin、GORM、WebSocket、JWT（Access/Refresh Token）、RESTful API、领域分层
- **数据与中间件**：MySQL、Redis（Pub/Sub、Sorted Set、Lua 令牌桶）、RabbitMQ（Publisher Confirm、TTL/DLX）、Elasticsearch
- **媒体与云服务**：FFmpeg/ffprobe、阿里云 OSS Presigned URL、Nginx、Docker Compose
- **前端**：Vue 3、TypeScript、Vite、Vue Router/Vuex、Element Plus、Vitest
- **工程化**：版本化数据库迁移、Outbox、幂等与状态机、Prometheus 指标、pprof、GitHub Actions、优雅关闭

## 项目经历

### cakecake 视频社区 | 独立开发 | Go + Vue 3 | [时间]

生产级视频分享社区，覆盖投稿、播放、实时弹幕、评论、全文搜索、热榜、私信与 AI 助手；采用单体分层架构，支持 Docker Compose 一键部署。

- **设计实时弹幕链路**：以 WebSocket Hub 按视频房间维护连接池，Redis Pub/Sub 负责跨实例广播，写入通道采用非阻塞投递并丢弃慢连接；连接时按播放时间窗口回放历史弹幕，端到端延迟目标低于 **200ms**，完成 **100 连接、约 400–500 msg/s** 压测且无意外错误。
- **搭建视频异步转码流水线**：浏览器通过 OSS Presigned PUT 直传，API 仅做对象归属与大小 HEAD 校验；以 MySQL Outbox 保证视频记录与任务原子提交，RabbitMQ + FFmpeg Worker 完成 H.264 转码、首帧截取和回传，上传接口从全量文件处理链路中解耦。
- **完善任务可靠性机制**：引入 Publisher Confirm、mandatory return、TTL/DLX 延迟重试、死信审计与人工重放；使用 `(job_id, retry_count)` 去重、状态机校验和条件更新，避免 at-least-once 投递造成重复转码及并发状态覆盖。
- **实现搜索与数据最终一致**：为视频、文章、用户建立 Elasticsearch 多字段索引和高亮查询；通过 `search_sync_outbox` 异步同步并指数退避，启动时支持全量 Reindex，ES 未配置时前端优雅降级。
- **开发 DeepSeek Agent 能力**：实现 OpenAI 兼容网关、流式打字机、停止/继续/重新生成及站内搜索/详情/榜单工具调用；通过 Redis 事件面、控制面与生成快照解耦多实例 WebSocket 和生成 Owner。
- **补齐生产工程能力**：实现 Redis Lua 令牌桶全局限流、优雅停机、Trace ID 贯穿、Prometheus 指标与 Grafana 告警；热榜 Redis 缓存优化后实测 QPS **412 → 3,601（8.7x）**，P99 **329ms → 30ms（-91%）**。
- **建立测试与交付流程**：后端使用 SQLite 内存库和 miniredis 编写单元测试，前端使用 Vitest；仓库提供 CI/CD 人工审批部署，单元测试语句覆盖率 **67.4%**，含集成测试总覆盖率 **70.7%**。

**项目链接**：https://github.com/earthcake2233/cakecake  
**在线体验**：https://chengzisoft.top/#/

## 教育经历

### [学校名称] | [专业] | [学历] | [起止时间]

[与目标岗位相关的课程、竞赛、奖项或研究方向]

## 其他信息

- 可使用 Docker Compose 启动 MySQL、Redis、RabbitMQ、Elasticsearch 及前后端服务。
- 熟悉 Linux/Nginx 基础部署、接口测试、性能压测与故障排查。
- 语言：[英语水平 / 技术阅读能力]

<!--
提交前请替换所有 [占位符]，并根据目标 JD 调整求职意向、技术关键词及项目要点。
以上项目指标来自仓库 README 与 docs/ARCHITECTURE.md；面试时应准备压测脚本、监控截图和关键代码位置以便核验。
-->
